# TODO

更新时间: 2026-07-10

来源: `PROJECT_KNOWLEDGE.md` 与 `fable-optimism-plan.md` 的未完成事项，并按当前代码重新核对。本文只保留尚未完成或仍需外部验收的工作。

## 当前基线

- 主词库 213 条，其中每日/默认练习题池 153 条。
- 提示猜词题库 241 条，默认可抽 236 条。
- 两个题池重合 139 条；主词库独有 74 条，提示题库独有 102 条，题池口径尚未合并。
- 已确认完成并移出 TODO：北京时间游戏日、固定种子无重复轮换、首启引导、首页玩法说明、百分制计分、方向锦囊、今日全网对比、昵称固化、最小埋点与全局异常上报、双人答案脱敏、重复加入/提示/结算防护、过期收桌、三种 Canvas 分享图及朋友圈分享代码、排行榜答案脱敏、主局服务端逐次判分、旧成绩直传拦截。
- 提示猜词 241 题已完成三轮人工校准和质量闸门；仍有 71 条旧数组题需要补 `source/storyHook/difficulty`，并等待真人盲测。
- 中文输入法候选栏修复已覆盖主局、提示猜词和双人猜词，并有自动回归检查；仍需在 iOS/Android 真机确认候选栏、键盘顶起和底部按钮位置。

## P0 上线前必须

- [ ] 微信后台与云开发上线配置
  - 部署当前前端实际调用的云函数：`manageRoom`、`getRoomState`、`submitHint`、`submitGuess`、`submitResult`、`getRanking`、`logEvent`、`subscribeDaily`。
  - `getDailyIdiom` 当前没有前端调用；切换主局云端取题或云端判分时再部署并接入。
  - 已确认创建 `daily_game_sessions`；继续校验 `idiom_rooms`、`idiom_hints_live`、`game_results`、`idiom_events`、`idiom_subscriptions` 是否都位于正式环境。
  - 将 `idiom_rooms`、`daily_game_sessions`、`game_results`、`idiom_events`、`idiom_subscriptions` 设为客户端不可直接读写；房间状态统一经云函数读取。`idiom_hints_live` 仅在需要客户端 `watch` 时开放最小只读权限，禁止客户端写入。
  - 为 `submitResult` 配置至少 32 字符的 `DAILY_GAME_SECRET`，为 `game_results` 创建 `date + verified + won + attempts + updatedAt` 复合索引；详细步骤见 `docs/p0-fair-ranking-deployment.md`。
  - 配置订阅消息模板 ID、激励视频广告位 ID、`subscribeDaily` 每日定时触发器，并确认正式环境云开发 ID。
  - 后台确认 `app_launch`、`enter_home`、`submit_guess`、`win/lose`、`share_tap/share_open`、`app_error` 能写入 `idiom_events`。

- [x] 排行榜公平性 P0（代码完成）
  - 今日榜接口和页面均不再返回或展示 `answerText`，云端失败不再用本地历史冒充全网榜。
  - 主局改为服务端开局、逐次判分与事务结算；旧的 `won/attempts/answerText` 直传被拒绝。
  - 可信成绩使用 `verified: true` 和确定性文档 ID；云不可用时明确降级为不计榜的本地练习。
  - 仍需完成上方微信后台配置和真机验收，才算生产环境 P0 关闭。

- [ ] 真机双人局与输入法全链路回归
  - A 建房，B 从分享进入，完成开局、提示、猜词、揭晓/下一局、结算和再开一桌。
  - 覆盖切后台重连、好友离线、房间过期、双方同时点下一局、重复提交、云函数失败和弱网恢复。
  - 抓取猜词者视角的 `getRoomState` 返回值，确认 `room.gameState.currentIdiom` 和 `myView.currentIdiom` 均为空。
  - 在 iOS 与 Android 分别验证三处猜词输入：拼音候选栏可见，选字后才进入四格，键盘弹起不遮挡清空/猜词按钮。
  - 验收标准：全链路无白屏、无重复结算、无跳局，控制台无未处理异常。

- [ ] 小程序 Logo / 图标重新设计
  - 产出小程序头像 144x144 PNG、分享封面 500x400 PNG、未来原生 tabBar 的 81x81 PNG 图标。
  - 风格沿用首页的暖蓝纸感、朱红与金色强调，避免办公软件感和纯文字占位图标。
  - 验证 PNG 单文件小于 40KB，在深浅背景和微信圆形裁切下都清晰可辨。

- [ ] 主词库扩到 500+ 条并统一题池口径
  - 当前主词库 213 条，距离目标还差至少 287 条；新增词条必须常见、有典故、普通玩家可猜。
  - 每条补齐拼音、部首、部首位置、结构、笔画、释义、出处和提示素材，并通过 `npm run lint:idioms`、`npm run sync:data`、`npm test`。
  - 明确主词库与提示题库的单一事实源，消除当前 74 条主词库独有、102 条提示题库独有的长期漂移。
  - 人工复核部首线索的可识别度，避免使用虽属字典部首、但普通玩家几乎看不出的弱提示。

## P1 灰度与增长

- [ ] 提示词来源补齐与真人盲测
  - 三轮人工校准已覆盖 241 题，并加入答案字、重复、泛化词、固定句式、跨题复用和同题包含式重复检查。
  - 将 71 条旧数组题转换为完整对象，为其补准确出处、典故文案和难度字段。
  - 抽样交给普通本科生成语水平玩家盲测，继续观察指向过强、过弱和同源典故互相干扰的问题。

- [ ] 题库开放能力产品专项
  - 明确题库是否可浏览、做题时是否禁止打开、是否通过看广告临时解锁。
  - 设计防复制、防剧透和不破坏每日挑战公平性的交互，再决定是否开发。

- [ ] 广告正式接入验收
  - 正式版验证只有完整看完激励视频才发放锦囊、补签或护盾；中途关闭不发奖励。
  - 明确开屏封面广告和插屏广告的投放节点、频控与首日用户保护策略。
  - 确认开发版/体验版降级逻辑不影响正式版收益路径，广告加载失败时文案和奖励策略符合预期。

- [ ] 分享与订阅链路真机验收
  - 验证好友点击房间分享卡片后，能携带房间码进入加入页。
  - 验证主局、提示猜词、双人局 Canvas 分享图在不同机型生成稳定，失败时能回退文字分享。
  - 验证朋友圈卡片样式、订阅授权、次日模板消息送达和同日防重复发送。

- [ ] 5-10 人灰度测试
  - 观察无需解释是否能玩明白、部首难度是否合理、提示词是否公平、锦囊是否有获得感、是否愿意分享。
  - 用 `idiom_events` 观察开始率、胜率、首猜后求助率、锦囊使用率、分享点击率、分享回流率、订阅授权率和次日回访率。
  - 记录机型、网络、失败步骤和房间码，形成可复现缺陷清单后再扩大体验成员范围。

- [ ] 双人异常流与调用量优化
  - 代码已具备过期收桌、重复加入/提示/结算防护和答案脱敏；剩余重点是好友离线、弱网恢复、下一局失败时的页面反馈。
  - 将房间固定 2 秒轮询改为自适应退避，例如活跃时 2 秒、空闲时 5 秒、连续无变化或后台时 10 秒。
  - 替换 `room-sync.js` 中已废弃的 `wx.getSystemInfoSync()`，统一使用新设备信息 API。

## P2 后续产品与架构

- [ ] 高难/典故挑战模式显式入口
  - 当前普通入口继续只抽大众题。
  - 后续可做“典故挑战”“高考成语”“三国周”等主题题包，并单独展示难度预期与战绩。

- [ ] 异步挑战、词卡册、成就墙与往期题归档
  - 异步挑战：发起挑战 -> 好友稍后完成 -> 双方看到比分卡。
  - 词卡册：猜过的成语自动收藏，展示释义、出处和典故。
  - 成就墙：首胜、连胜、一次猜中、零提示破题等徽章。
  - 往期题归档：可回玩但不计连胜，不泄露当天答案。

- [ ] 结构还债
  - 建立 `shared/` 纯函数管线，统一计分、日期、题池筛选和房间过期逻辑，并由脚本同步到独立云函数。
  - 将四格输入优先抽成组件，随后组件化结算弹层、说明弹层和底部导航；中文输入法修复不再维护三份。
  - 评估启用原生 tabBar；若保留自绘导航，至少减少 `reLaunch` 导致的页面重建和闪白。
  - 将云环境 ID 从 `app.js` 提取到配置层，区分开发、体验和正式环境。
  - 本地历史记录限制为最近 365 条，避免 `idiom_wordle_history` 无限增长。
  - 将健康检查中的具体色值/间距断言收敛为设计 token 和行为断言。
  - 解决 `submitHint` 房间缓存与 `idiom_hints_live` 双写失败时的一致性问题。

- [ ] 权益状态反作弊终态
  - 主局云端判分、隐藏每日答案和可信成绩入榜已经完成；正式环境仍需配置密钥、索引并部署验收。
  - 连胜、护盾和补签等可影响排行或权益的数据迁到云端可信状态。
